import React, { Component } from 'react';
import MasterComponent from '../components/Layout/MasterComponent';

class PropertyListings extends Component {
    render() {
        return (
            <div>
                Property pipe line
            </div>
        );
    }
}

export default MasterComponent(PropertyListings);