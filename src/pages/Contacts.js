import React, { Component } from 'react';
import MasterComponent from '../components/Layout/MasterComponent';

class Contacts extends Component {
    render() {
        return (
            <div>
                Contacts
            </div>
        );
    }
}

export default MasterComponent(Contacts);